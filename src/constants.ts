/** Default block duration for new blocks when no explicit end is set. */
export const BLOCK_DURATION_MIN = 150;

/** Latest allowed start minute in the same calendar day. */
export const MAX_BLOCK_START_MIN = 24 * 60 - 1;
